"""STUB — Phase 1 task 1.4 (not part of the laptop vertical slice).

Will seal an AES key to a PCR-10 policy (tpm2_createpolicy --policy-pcr),
so tpm2_unseal only succeeds while PCR 10 matches the known-good state.
payload/play_video.py consumes the unsealed key.
"""

raise NotImplementedError("Phase 1 task 1.4 — see docs/SPEC_EN.md §8")
